local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local HttpService = game:GetService("HttpService")
local cloneref = cloneref or function(o) return o end
local SetClipboard = toclipboard or setclipboard or (syn and syn.set_clipboard)

local LocalPlayer = Players.LocalPlayer

-- Remove old UI if exists to prevent duplicates
if CoreGui:FindFirstChild("WaiiHub_UI") then
    CoreGui.WaiiHub_UI:Destroy()
end

-- Create main ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "WaiiHub_UI"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true

-- Background Overlay
local Overlay = Instance.new("Frame")
Overlay.Name = "Overlay"
Overlay.Size = UDim2.new(1, 0, 1, 0)
Overlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Overlay.BackgroundTransparency = 1
Overlay.BorderSizePixel = 0
Overlay.Parent = ScreenGui

-- Main Interface Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 550, 0, 340)
MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Size = UDim2.new(0, 0, 0, 0)
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 18)
UICorner.Parent = MainFrame

-- ==========================================
-- FLOATING ROUND TOGGLE BUTTON
-- ==========================================
local FloatingToggleBtn = Instance.new("ImageButton")
FloatingToggleBtn.Name = "FloatingToggleBtn"
FloatingToggleBtn.Size = UDim2.new(0, 48, 0, 48)
FloatingToggleBtn.Position = UDim2.new(0, 25, 0.4, 0)
FloatingToggleBtn.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
FloatingToggleBtn.Image = "rbxassetid://120937441097851"
FloatingToggleBtn.ScaleType = Enum.ScaleType.Stretch
FloatingToggleBtn.Visible = true
FloatingToggleBtn.Parent = ScreenGui

local FloatCorner = Instance.new("UICorner")
FloatCorner.CornerRadius = UDim.new(1, 0)
FloatCorner.Parent = FloatingToggleBtn

local FloatStroke = Instance.new("UIStroke")
FloatStroke.Color = Color3.fromRGB(255, 255, 255)
FloatStroke.Transparency = 0.2
FloatStroke.Thickness = 1.5
FloatStroke.Parent = FloatingToggleBtn

local fDragging, fDragInput, fDragStart, fStartPos, fHasMoved = false, nil, nil, nil, false
FloatingToggleBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        fDragging = true
        fHasMoved = false
        fDragStart = input.Position
        fStartPos = FloatingToggleBtn.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                fDragging = false
            end
        end)
    end
end)

FloatingToggleBtn.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        fDragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == fDragInput and fDragging then
        local delta = input.Position - fDragStart
        if delta.Magnitude > 5 then
            fHasMoved = true
        end
        FloatingToggleBtn.Position = UDim2.new(
            fStartPos.X.Scale, fStartPos.X.Offset + delta.X,
            fStartPos.Y.Scale, fStartPos.Y.Offset + delta.Y
        )
    end
end)

-- ==========================================
-- BACKGROUND IMAGE FOR MAIN MENU
-- ==========================================
local MenuBackgroundImage = Instance.new("ImageLabel")
MenuBackgroundImage.Name = "MenuBackgroundImage"
MenuBackgroundImage.Size = UDim2.new(1, 0, 1, 0)
MenuBackgroundImage.BackgroundTransparency = 1
MenuBackgroundImage.Image = "rbxassetid://120937441097851"
MenuBackgroundImage.ScaleType = Enum.ScaleType.Stretch
MenuBackgroundImage.ImageTransparency = 0.35
MenuBackgroundImage.Parent = MainFrame

local BgImageCorner = Instance.new("UICorner")
BgImageCorner.CornerRadius = UDim.new(0, 18)
BgImageCorner.Parent = MenuBackgroundImage

local UIStroke = Instance.new("UIStroke")
UIStroke.Color = Color3.fromRGB(255, 255, 255)
UIStroke.Transparency = 0.5
UIStroke.Thickness = 1.2
UIStroke.Parent = MainFrame

local StrokeGradient = Instance.new("UIGradient")
StrokeGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
    ColorSequenceKeypoint.new(0.25, Color3.fromRGB(80, 80, 80)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(20, 20, 20)),
    ColorSequenceKeypoint.new(0.75, Color3.fromRGB(80, 80, 80)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 255, 255))
})
StrokeGradient.Parent = UIStroke

task.spawn(function()
    local rotation = 0
    while true do
        rotation = (rotation + 0.5) % 360
        StrokeGradient.Rotation = rotation
        RunService.RenderStepped:Wait()
    end
end)

-- ==========================================
-- TITLE CONTAINER (VỚI KHUNG VIỀN BAO QUANH)
-- ==========================================
local TitleContainer = Instance.new("Frame")
TitleContainer.Size = UDim2.new(0, 110, 0, 30)
TitleContainer.Position = UDim2.new(0, 16, 0, 17)
TitleContainer.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
TitleContainer.BackgroundTransparency = 0.3
TitleContainer.BorderSizePixel = 0
TitleContainer.Parent = MainFrame

local TitleContainerCorner = Instance.new("UICorner")
TitleContainerCorner.CornerRadius = UDim.new(0, 8)
TitleContainerCorner.Parent = TitleContainer

local TitleContainerStroke = Instance.new("UIStroke")
TitleContainerStroke.Color = Color3.fromRGB(50, 50, 50)
TitleContainerStroke.Transparency = 0.4
TitleContainerStroke.Thickness = 1.2
TitleContainerStroke.Parent = TitleContainer

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 1, 0)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.GothamBold
Title.Text = 'Waii Hub'
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 13
Title.Parent = TitleContainer

-- ==========================================
-- FPS DISPLAY CONTAINER (NẰM BÊN CẠNH NÚT ĐÓNG)
-- ==========================================
local FpsContainer = Instance.new("Frame")
FpsContainer.Size = UDim2.new(0, 75, 0, 28)
FpsContainer.Position = UDim2.new(1, -125, 0, 18)
FpsContainer.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
FpsContainer.BackgroundTransparency = 0.3
FpsContainer.BorderSizePixel = 0
FpsContainer.Parent = MainFrame

local FpsCorner = Instance.new("UICorner")
FpsCorner.CornerRadius = UDim.new(1, 0)
FpsCorner.Parent = FpsContainer

local FpsStroke = Instance.new("UIStroke")
FpsStroke.Color = Color3.fromRGB(50, 50, 50)
FpsStroke.Transparency = 0.4
FpsStroke.Thickness = 1.2
FpsStroke.Parent = FpsContainer

local FpsLabel = Instance.new("TextLabel")
FpsLabel.Size = UDim2.new(1, 0, 1, 0)
FpsLabel.BackgroundTransparency = 1
FpsLabel.Font = Enum.Font.GothamMedium
FpsLabel.Text = "FPS: 0"
FpsLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
FpsLabel.TextSize = 11
FpsLabel.Parent = FpsContainer

-- Cập nhật FPS liên tục
task.spawn(function()
    local lastUpdate = tick()
    local frames = 0
    RunService.RenderStepped:Connect(function()
        frames = frames + 1
        local now = tick()
        if now - lastUpdate >= 1 then
            local fps = math.floor(frames / (now - lastUpdate))
            FpsLabel.Text = "FPS: " .. fps
            frames = 0
            lastUpdate = now
        end
    end)
end)

-- Nút Đóng (X)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 28, 0, 28)
CloseBtn.Position = UDim2.new(1, -40, 0, 18)
CloseBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
CloseBtn.BackgroundTransparency = 0.3
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
CloseBtn.TextSize = 13
CloseBtn.Parent = MainFrame

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(1, 0)
CloseCorner.Parent = CloseBtn

-- ==========================================
-- SIDEBAR CONTAINER
-- ==========================================
local SidebarContainer = Instance.new("Frame")
SidebarContainer.Name = "SidebarContainer"
SidebarContainer.Size = UDim2.new(0, 125, 1, -74)
SidebarContainer.Position = UDim2.new(0, 16, 0, 56)
SidebarContainer.BackgroundColor3 = Color3.fromRGB(18, 18, 18) 
SidebarContainer.BackgroundTransparency = 0.3
SidebarContainer.BorderSizePixel = 0
SidebarContainer.Parent = MainFrame

local SidebarContainerCorner = Instance.new("UICorner")
SidebarContainerCorner.CornerRadius = UDim.new(0, 14)
SidebarContainerCorner.Parent = SidebarContainer

local SidebarContainerStroke = Instance.new("UIStroke")
SidebarContainerStroke.Color = Color3.fromRGB(45, 45, 45) 
SidebarContainerStroke.Transparency = 0.5
SidebarContainerStroke.Thickness = 1
SidebarContainerStroke.Parent = SidebarContainer

local Sidebar = Instance.new("ScrollingFrame")
Sidebar.Size = UDim2.new(1, -12, 1, -65)
Sidebar.Position = UDim2.new(0, 6, 0, 10)
Sidebar.BackgroundTransparency = 1
Sidebar.BorderSizePixel = 0
Sidebar.ScrollBarThickness = 2
Sidebar.CanvasSize = UDim2.new(0, 0, 0, 180)
Sidebar.Parent = SidebarContainer

local SidebarLayout = Instance.new("UIListLayout")
SidebarLayout.SortOrder = Enum.SortOrder.LayoutOrder
SidebarLayout.Padding = UDim.new(0, 6)
SidebarLayout.Parent = Sidebar

local TabMainBtn = Instance.new("TextButton")
TabMainBtn.Size = UDim2.new(1, 0, 0, 32)
TabMainBtn.BackgroundColor3 = Color3.fromRGB(235, 235, 235)
TabMainBtn.BackgroundTransparency = 0.05
TabMainBtn.Font = Enum.Font.GothamBold
TabMainBtn.Text = "  Main"
TabMainBtn.TextColor3 = Color3.fromRGB(12, 12, 12)
TabMainBtn.TextSize = 12
TabMainBtn.TextXAlignment = Enum.TextXAlignment.Left
TabMainBtn.LayoutOrder = 1
TabMainBtn.Parent = Sidebar

local TabMainCorner = Instance.new("UICorner")
TabMainCorner.CornerRadius = UDim.new(0, 8)
TabMainCorner.Parent = TabMainBtn

-- ==========================================
-- CONTENT CONTAINER
-- ==========================================
local ContentContainer = Instance.new("Frame")
ContentContainer.Name = "ContentContainer"
ContentContainer.Size = UDim2.new(1, -170, 1, -75)
ContentContainer.Position = UDim2.new(0, 155, 0, 65)
ContentContainer.BackgroundTransparency = 1
ContentContainer.ClipsDescendants = false
ContentContainer.Parent = MainFrame

local MainContent = Instance.new("ScrollingFrame")
MainContent.Size = UDim2.new(1, 0, 1, 0)
MainContent.BackgroundTransparency = 1
MainContent.BorderSizePixel = 0
MainContent.ScrollBarThickness = 3
MainContent.CanvasSize = UDim2.new(0, 0, 0, 350)
MainContent.Visible = true
MainContent.Parent = ContentContainer

local MainLayout = Instance.new("UIListLayout")
MainLayout.SortOrder = Enum.SortOrder.LayoutOrder
MainLayout.Padding = UDim.new(0, 10)
MainLayout.Parent = MainContent

MainLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    MainContent.CanvasSize = UDim2.new(0, 0, 0, MainLayout.AbsoluteContentSize.Y + 15)
end)

local MainWrapper = Instance.new("Frame")
MainWrapper.Size = UDim2.new(1, 0, 1, 0)
MainWrapper.BackgroundTransparency = 1
MainWrapper.Parent = MainContent

local AvatarContainer = Instance.new("Frame")
AvatarContainer.Size = UDim2.new(0, 30, 0, 30)
AvatarContainer.Position = UDim2.new(0, 8, 1, -38)
AvatarContainer.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
AvatarContainer.BackgroundTransparency = 0.25
AvatarContainer.BorderSizePixel = 0
AvatarContainer.Parent = SidebarContainer

local AvatarCorner = Instance.new("UICorner")
AvatarCorner.CornerRadius = UDim.new(1, 0)
AvatarCorner.Parent = AvatarContainer

local AvatarStroke = Instance.new("UIStroke")
AvatarStroke.Color = Color3.fromRGB(70, 70, 70)
AvatarStroke.Thickness = 1.5
AvatarStroke.Parent = AvatarContainer

local AvatarImage = Instance.new("ImageLabel")
AvatarImage.Size = UDim2.new(1, 0, 1, 0)
AvatarImage.BackgroundTransparency = 1
AvatarImage.Image = "rbxthumb://type=AvatarHeadShot&id=" .. LocalPlayer.UserId .. "&w=150&h=150"
AvatarImage.Parent = AvatarContainer

local ImageCorner = Instance.new("UICorner")
ImageCorner.CornerRadius = UDim.new(1, 0)
ImageCorner.Parent = AvatarImage

local UsernameLabel = Instance.new("TextLabel")
UsernameLabel.Size = UDim2.new(0, 70, 0, 15)
UsernameLabel.Position = UDim2.new(0, 42, 1, -38)
UsernameLabel.BackgroundTransparency = 1
UsernameLabel.Font = Enum.Font.GothamBold
UsernameLabel.Text = LocalPlayer.Name
UsernameLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
UsernameLabel.TextSize = 11
UsernameLabel.TextXAlignment = Enum.TextXAlignment.Left
UsernameLabel.TextTruncate = Enum.TextTruncate.AtEnd
UsernameLabel.Parent = SidebarContainer

local TimeLabel = Instance.new("TextLabel")
TimeLabel.Size = UDim2.new(0, 70, 0, 15)
TimeLabel.Position = UDim2.new(0, 42, 1, -23)
TimeLabel.BackgroundTransparency = 1
TimeLabel.Font = Enum.Font.GothamMedium
TimeLabel.TextColor3 = Color3.fromRGB(140, 140, 140)
TimeLabel.TextSize = 10
TimeLabel.TextXAlignment = Enum.TextXAlignment.Left
TimeLabel.Parent = SidebarContainer

task.spawn(function()
    while true do
        TimeLabel.Text = os.date("%H:%M:%S")
        task.wait(1)
    end
end)

-- ==========================================
-- DRAGGABLE LOGIC FOR MAIN FRAME
-- ==========================================
local dragging, dragInput, dragStart, startPos

MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(
            startPos.X.Scale, startPos.X.Offset + delta.X, 
            startPos.Y.Scale, startPos.Y.Offset + delta.Y
        )
    end
end)

-- ==========================================
-- TOGGLE MENU VISIBILITY LOGIC
-- ==========================================
local isHidden = false

-- Floating Toggle Button Logic
local FloatingToggleBtn = Instance.new("ImageButton")
FloatingToggleBtn.Name = "FloatingToggleBtn"
FloatingToggleBtn.Size = UDim2.new(0, 48, 0, 48)
FloatingToggleBtn.Position = UDim2.new(0, 25, 0.4, 0)
FloatingToggleBtn.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
FloatingToggleBtn.Image = "rbxassetid://120937441097851"
FloatingToggleBtn.ScaleType = Enum.ScaleType.Stretch
FloatingToggleBtn.Visible = true
FloatingToggleBtn.Parent = ScreenGui

local FloatCorner = Instance.new("UICorner")
FloatCorner.CornerRadius = UDim.new(1, 0)
FloatCorner.Parent = FloatingToggleBtn

local FloatStroke = Instance.new("UIStroke")
FloatStroke.Color = Color3.fromRGB(255, 255, 255)
FloatStroke.Transparency = 0.2
FloatStroke.Thickness = 1.5
FloatStroke.Parent = FloatingToggleBtn

local fDragging, fDragInput, fDragStart, fStartPos, fHasMoved = false, nil, nil, nil, false
FloatingToggleBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        fDragging = true
        fHasMoved = false
        fDragStart = input.Position
        fStartPos = FloatingToggleBtn.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                fDragging = false
            end
        end)
    end
end)

FloatingToggleBtn.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        fDragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == fDragInput and fDragging then
        local delta = input.Position - fDragStart
        if delta.Magnitude > 5 then
            fHasMoved = true
        end
        FloatingToggleBtn.Position = UDim2.new(
            fStartPos.X.Scale, fStartPos.X.Offset + delta.X,
            fStartPos.Y.Scale, fStartPos.Y.Offset + delta.Y
        )
    end
end)

FloatingToggleBtn.MouseButton1Click:Connect(function()
    if fHasMoved then return end
    
    isHidden = not isHidden
    if isHidden then
        TweenService:Create(MainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0)}):Play()
        TweenService:Create(Overlay, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {BackgroundTransparency = 1}):Play()
        task.delay(0.25, function()
            if isHidden then Overlay.Visible = false end
        end)
    else
        Overlay.Visible = true
        TitleContainer.Visible = true
        SidebarContainer.Visible = true
        ContentContainer.Visible = true
        TweenService:Create(Overlay, TweenInfo.new(0.3), {BackgroundTransparency = 0.5}):Play()
        TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 550, 0, 340)}):Play()
    end
end)

task.spawn(function()
    Overlay.Visible = true
    TweenService:Create(Overlay, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        BackgroundTransparency = 0.5
    }):Play()

    MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
    MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)

    local openTween = TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 550, 0, 340)
    })
    openTween:Play()
end)

CloseBtn.MouseButton1Click:Connect(function()
    TweenService:Create(MainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 0, 0, 0)
    }):Play()
    
    TweenService:Create(Overlay,TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
        BackgroundTransparency = 1
    }):Play()

    task.wait(0.25)
    ScreenGui:Destroy()
end)

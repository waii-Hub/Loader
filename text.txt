-- // waii Hub UI Script (Monochrome Theme + Custom Rounded Background Image + Live FPS Counter)
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

-- Xóa UI cũ nếu có để tránh trùng lặp
if CoreGui:FindFirstChild("WaiiHubUI") then
    CoreGui.WaiiHubUI:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "WaiiHubUI"
ScreenGui.Parent = RunService:IsStudio() and game.Players.LocalPlayer:WaitForChild("PlayerGui") or CoreGui
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true

-- Khung Menu Chính
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
MainFrame.Size = UDim2.new(0, 0, 0, 0)
MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
MainFrame.ClipsDescendants = true
MainFrame.BackgroundTransparency = 1

local Corner = Instance.new("UICorner", MainFrame)
Corner.CornerRadius = UDim.new(0, 12)

local Stroke = Instance.new("UIStroke", MainFrame)
Stroke.Color = Color3.fromRGB(50, 50, 50)
Stroke.Transparency = 1
Stroke.Thickness = 1.2

-- Hiệu ứng bóng đổ nhẹ cho Menu chính
local Shadow = Instance.new("ImageLabel", MainFrame)
Shadow.Name = "Shadow"
Shadow.AnchorPoint = Vector2.new(0.5, 0.5)
Shadow.BackgroundTransparency = 1
Shadow.Position = UDim2.new(0.5, 0, 0.5, 0)
Shadow.Size = UDim2.new(1, 30, 1, 30)
Shadow.ZIndex = -1
Shadow.Image = "rbxassetid://6014261993"
Shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
Shadow.ImageTransparency = 0.5
Shadow.ScaleType = Enum.ScaleType.Slice
Shadow.SliceCenter = Rect.new(49, 49, 450, 450)

-- // HÌNH NỀN CHÍNH (Áp dụng ID ảnh + Bo góc ôm trọn khung menu)
local BgImage = Instance.new("ImageLabel", MainFrame)
BgImage.Name = "BgImage"
BgImage.Size = UDim2.new(1, 0, 1, 0)
BgImage.BackgroundTransparency = 1
BgImage.Image = "rbxassetid://120937441097851"
BgImage.ScaleType = Enum.ScaleType.Crop
BgImage.ImageTransparency = 1
BgImage.ZIndex = 1

local BgCorner = Instance.new("UICorner", BgImage)
BgCorner.CornerRadius = UDim.new(0, 12)

-- Lớp phủ tối nhẹ (Overlay)
local BgOverlay = Instance.new("Frame", MainFrame)
BgOverlay.Name = "BgOverlay"
BgOverlay.Size = UDim2.new(1, 0, 1, 0)
BgOverlay.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
BgOverlay.BackgroundTransparency = 1
BgOverlay.ZIndex = 2

local OverlayCorner = Instance.new("UICorner", BgOverlay)
OverlayCorner.CornerRadius = UDim.new(0, 12)

-- // MÀN HÌNH LOADING
local LoadingFrame = Instance.new("Frame", MainFrame)
LoadingFrame.Name = "LoadingFrame"
LoadingFrame.Size = UDim2.new(1, 0, 1, 0)
LoadingFrame.BackgroundTransparency = 1
LoadingFrame.ZIndex = 5

local LoadingTitle = Instance.new("TextLabel", LoadingFrame)
LoadingTitle.Size = UDim2.new(1, 0, 0, 30)
LoadingTitle.Position = UDim2.new(0, 0, 0.5, -50)
LoadingTitle.BackgroundTransparency = 1
LoadingTitle.Text = "Waii Hub Community"
LoadingTitle.Font = Enum.Font.GothamBold
LoadingTitle.TextSize = 18
LoadingTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
LoadingTitle.TextTransparency = 1
LoadingTitle.ZIndex = 6

local StatusText = Instance.new("TextLabel", LoadingFrame)
StatusText.Size = UDim2.new(1, 0, 0, 20)
StatusText.Position = UDim2.new(0, 0, 0.5, -15)
StatusText.BackgroundTransparency = 1
StatusText.Text = "Welcome"
StatusText.Font = Enum.Font.Gotham
StatusText.TextSize = 13
StatusText.TextColor3 = Color3.fromRGB(200, 200, 200)
StatusText.TextTransparency = 1
StatusText.ZIndex = 6

local BarBackground = Instance.new("Frame", LoadingFrame)
BarBackground.Size = UDim2.new(0, 220, 0, 4)
BarBackground.Position = UDim2.new(0.5, -110, 0.5, 15)
BarBackground.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
BarBackground.BorderSizePixel = 0
BarBackground.BackgroundTransparency = 1
BarBackground.ZIndex = 6
Instance.new("UICorner", BarBackground).CornerRadius = UDim.new(1, 0)

local BarFill = Instance.new("Frame", BarBackground)
BarFill.Size = UDim2.new(0, 0, 1, 0)
BarFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
BarFill.BorderSizePixel = 0
BarFill.ZIndex = 7
Instance.new("UICorner", BarFill).CornerRadius = UDim.new(1, 0)

-- // THANH TIÊU ĐỀ (TopBar)
local TopBar = Instance.new("Frame", MainFrame)
TopBar.Name = "TopBar"
TopBar.BackgroundTransparency = 1
TopBar.Size = UDim2.new(1, 0, 0, 48)
TopBar.Active = true
TopBar.Visible = false
TopBar.ZIndex = 10

local Title = Instance.new("TextLabel", TopBar)
Title.Name = "HubTitle"
Title.Text = "  waii Hub"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 15
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Size = UDim2.new(0, 130, 1, 0)
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.ZIndex = 10

local SearchBox = Instance.new("TextBox", TopBar)
SearchBox.Name = "SearchBox"
SearchBox.Size = UDim2.new(0, 210, 0, 32)
SearchBox.Position = UDim2.new(0, 125, 0.5, -16)
SearchBox.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
SearchBox.PlaceholderText = "Search features..."
SearchBox.PlaceholderColor3 = Color3.fromRGB(110, 110, 110)
SearchBox.Text = ""
SearchBox.TextColor3 = Color3.fromRGB(240, 240, 240)
SearchBox.Font = Enum.Font.Gotham
SearchBox.TextSize = 13
SearchBox.TextXAlignment = Enum.TextXAlignment.Left
SearchBox.ZIndex = 10

Instance.new("UICorner", SearchBox).CornerRadius = UDim.new(0, 8)

local SearchPadding = Instance.new("UIPadding", SearchBox)
SearchPadding.PaddingLeft = UDim.new(0, 14)

local SearchStroke = Instance.new("UIStroke", SearchBox)
SearchStroke.Color = Color3.fromRGB(40, 40, 40)
SearchStroke.Thickness = 1

SearchBox.Focused:Connect(function()
    TweenService:Create(SearchStroke, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Color = Color3.fromRGB(255, 255, 255)}):Play()
end)
SearchBox.FocusLost:Connect(function()
    TweenService:Create(SearchStroke, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Color = Color3.fromRGB(40, 40, 40)}):Play()
end)

-- Nút Đóng (X)
local CloseBtn = Instance.new("TextButton", TopBar)
CloseBtn.Name = "CloseBtn"
CloseBtn.AnchorPoint = Vector2.new(0.5, 0.5)
CloseBtn.Position = UDim2.new(1, -24, 0.5, 0)
CloseBtn.Size = UDim2.new(0, 28, 0, 28)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(160, 160, 160)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 14
CloseBtn.AutoButtonColor = false
CloseBtn.ZIndex = 10

CloseBtn.MouseEnter:Connect(function()
    TweenService:Create(CloseBtn, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
end)
CloseBtn.MouseLeave:Connect(function()
    TweenService:Create(CloseBtn, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {TextColor3 = Color3.fromRGB(160, 160, 160)}):Play()
end)

-- Nút Thu Nhỏ (-)
local MinimizeBtn = Instance.new("TextButton", TopBar)
MinimizeBtn.Name = "MinimizeBtn"
MinimizeBtn.AnchorPoint = Vector2.new(0.5, 0.5)
MinimizeBtn.Position = UDim2.new(1, -56, 0.5, 0)
MinimizeBtn.Size = UDim2.new(0, 28, 0, 28)
MinimizeBtn.BackgroundTransparency = 1
MinimizeBtn.Text = "-"
MinimizeBtn.TextColor3 = Color3.fromRGB(160, 160, 160)
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.TextSize = 16
MinimizeBtn.AutoButtonColor = false
MinimizeBtn.ZIndex = 10

MinimizeBtn.MouseEnter:Connect(function()
    TweenService:Create(MinimizeBtn, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
end)
MinimizeBtn.MouseLeave:Connect(function()
    TweenService:Create(MinimizeBtn, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {TextColor3 = Color3.fromRGB(160, 160, 160)}):Play()
end)

-- // KHUNG HIỂN THỊ FPS (Nằm bên phải nút thu nhỏ)
local FpsLabel = Instance.new("TextLabel", TopBar)
FpsLabel.Name = "FpsLabel"
FpsLabel.AnchorPoint = Vector2.new(0.5, 0.5)
FpsLabel.Position = UDim2.new(1, -95, 0.5, 0)
FpsLabel.Size = UDim2.new(0, 48, 0, 22)
FpsLabel.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
FpsLabel.BackgroundTransparency = 0.5
FpsLabel.Text = "FPS: 60"
FpsLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
FpsLabel.Font = Enum.Font.GothamBold
FpsLabel.TextSize = 11
FpsLabel.ZIndex = 10
Instance.new("UICorner", FpsLabel).CornerRadius = UDim.new(0, 6)

-- Cập nhật FPS liên tục theo thời gian thực
local lastUpdate = 0
RunService.RenderStepped:Connect(function(dt)
    if tick() - lastUpdate >= 0.5 then
        lastUpdate = tick()
        local fps = math.floor(1 / dt + 0.5)
        FpsLabel.Text = "FPS: " .. fps
    end
end)

-- // KHUNG CHỨA NỘI DUNG (CONTENT)
local ContentContainer = Instance.new("Frame", MainFrame)
ContentContainer.Name = "ContentContainer"
ContentContainer.BackgroundTransparency = 1
ContentContainer.Size = UDim2.new(1, 0, 1, -48)
ContentContainer.Position = UDim2.new(0, 0, 0, 48)
ContentContainer.Visible = false
ContentContainer.ZIndex = 10

local Sidebar = Instance.new("ScrollingFrame", ContentContainer)
Sidebar.Name = "Sidebar"
Sidebar.BackgroundTransparency = 1
Sidebar.Size = UDim2.new(0, 160, 1, 0)
Sidebar.Position = UDim2.new(0, 0, 0, 0)
Sidebar.ScrollBarThickness = 0
Sidebar.CanvasSize = UDim2.new(0, 0, 0, 0)
Sidebar.ZIndex = 10

local SidebarLayout = Instance.new("UIListLayout", Sidebar)
SidebarLayout.SortOrder = Enum.SortOrder.LayoutOrder
SidebarLayout.Padding = UDim.new(0, 6)

local SidebarPadding = Instance.new("UIPadding", Sidebar)
SidebarPadding.PaddingLeft = UDim.new(0, 12)
SidebarPadding.PaddingTop = UDim.new(0, 12)

local Divider = Instance.new("Frame", ContentContainer)
Divider.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
Divider.BorderSizePixel = 0
Divider.Size = UDim2.new(0, 1, 1, 0)
Divider.Position = UDim2.new(0, 165, 0, 0)
Divider.ZIndex = 10

local PageContainer = Instance.new("Frame", ContentContainer)
PageContainer.Name = "PageContainer"
PageContainer.BackgroundTransparency = 1
PageContainer.Size = UDim2.new(1, -175, 1, 0)
PageContainer.Position = UDim2.new(0, 175, 0, 0)
PageContainer.ZIndex = 10

-- // ICON VUÔNG KHI THU NHỎ
local FloatingIcon = Instance.new("TextButton", ScreenGui)
FloatingIcon.Name = "FloatingIcon"
FloatingIcon.Size = UDim2.new(0, 45, 0, 45)
FloatingIcon.Position = UDim2.new(0.05, 0, 0.15, 0)
FloatingIcon.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
FloatingIcon.Text = "W"
FloatingIcon.TextColor3 = Color3.fromRGB(255, 255, 255)
FloatingIcon.Font = Enum.Font.GothamBold
FloatingIcon.TextSize = 16
FloatingIcon.Visible = false
FloatingIcon.AutoButtonColor = false

Instance.new("UICorner", FloatingIcon).CornerRadius = UDim.new(0, 10)
local FloatStroke = Instance.new("UIStroke", FloatingIcon)
FloatStroke.Color = Color3.fromRGB(50, 50, 50)
FloatStroke.Thickness = 1.2

FloatingIcon.MouseEnter:Connect(function()
    TweenService:Create(FloatingIcon, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(30, 30, 30)}):Play()
end)
FloatingIcon.MouseLeave:Connect(function()
    TweenService:Create(FloatingIcon, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(12, 12, 12)}):Play()
end)

-- // MENU XÁC NHẬN (CONFIRMATION DIALOG)
local ConfirmOverlay = Instance.new("Frame", ScreenGui)
ConfirmOverlay.Name = "ConfirmOverlay"
ConfirmOverlay.Size = UDim2.new(1, 0, 1, 0)
ConfirmOverlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ConfirmOverlay.BackgroundTransparency = 1
ConfirmOverlay.Visible = false

local ConfirmBox = Instance.new("Frame", ConfirmOverlay)
ConfirmBox.Name = "ConfirmBox"
ConfirmBox.AnchorPoint = Vector2.new(0.5, 0.5)
ConfirmBox.Position = UDim2.new(0.5, 0, 0.5, 0)
ConfirmBox.Size = UDim2.new(0, 340, 0, 160)
ConfirmBox.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
ConfirmBox.BackgroundTransparency = 1
ConfirmBox.ClipsDescendants = true

Instance.new("UICorner", ConfirmBox).CornerRadius = UDim.new(0, 12)
local ConfirmStroke = Instance.new("UIStroke", ConfirmBox)
ConfirmStroke.Color = Color3.fromRGB(50, 50, 50)
ConfirmStroke.Transparency = 1
ConfirmStroke.Thickness = 1.2

local ConfirmShadow = Instance.new("ImageLabel", ConfirmBox)
ConfirmShadow.AnchorPoint = Vector2.new(0.5, 0.5)
ConfirmShadow.BackgroundTransparency = 1
ConfirmShadow.Position = UDim2.new(0.5, 0, 0.5, 0)
ConfirmShadow.Size = UDim2.new(1, 30, 1, 30)
ConfirmShadow.ZIndex = -1
ConfirmShadow.Image = "rbxassetid://6014261993"
ConfirmShadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
ConfirmShadow.ImageTransparency = 0.5
ConfirmShadow.ScaleType = Enum.ScaleType.Slice
ConfirmShadow.SliceCenter = Rect.new(49, 49, 450, 450)

local ConfirmText = Instance.new("TextLabel", ConfirmBox)
ConfirmText.Size = UDim2.new(1, 0, 0, 60)
ConfirmText.Position = UDim2.new(0, 0, 0, 18)
ConfirmText.BackgroundTransparency = 1
ConfirmText.Text = "Do you want to delete the menu?"
ConfirmText.TextColor3 = Color3.fromRGB(255, 255, 255)
ConfirmText.Font = Enum.Font.GothamBold
ConfirmText.TextSize = 15

local YesBtn = Instance.new("TextButton", ConfirmBox)
YesBtn.Name = "YesBtn"
YesBtn.Size = UDim2.new(0, 130, 0, 40)
YesBtn.Position = UDim2.new(0.5, -138, 0.62, 0)
YesBtn.BackgroundColor3 = Color3.fromRGB(240, 240, 240)
YesBtn.Text = "Yes"
YesBtn.TextColor3 = Color3.fromRGB(15, 15, 15)
YesBtn.Font = Enum.Font.GothamBold
YesBtn.TextSize = 14
YesBtn.AutoButtonColor = false
Instance.new("UICorner", YesBtn).CornerRadius = UDim.new(0, 8)

YesBtn.MouseEnter:Connect(function()
    TweenService:Create(YesBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
end)
YesBtn.MouseLeave:Connect(function()
    TweenService:Create(YesBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(240, 240, 240)}):Play()
end)

local NoBtn = Instance.new("TextButton", ConfirmBox)
NoBtn.Name = "NoBtn"
NoBtn.Size = UDim2.new(0, 130, 0, 40)
NoBtn.Position = UDim2.new(0.5, 8, 0.62, 0)
NoBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
NoBtn.Text = "No"
NoBtn.TextColor3 = Color3.fromRGB(220, 220, 220)
NoBtn.Font = Enum.Font.GothamBold
NoBtn.TextSize = 14
NoBtn.AutoButtonColor = false
Instance.new("UICorner", NoBtn).CornerRadius = UDim.new(0, 8)

NoBtn.MouseEnter:Connect(function()
    TweenService:Create(NoBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(45, 45, 45)}):Play()
end)
NoBtn.MouseLeave:Connect(function()
    TweenService:Create(NoBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(30, 30, 30)}):Play()
end)

-- // ANIMATION MỞ MENU VÀ LOADING
task.spawn(function()
    TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 650, 0, 420),
        Position = UDim2.new(0.5, -325, 0.5, -210),
        BackgroundTransparency = 0
    }):Play()
    TweenService:Create(Stroke, TweenInfo.new(0.4), {Transparency = 0}):Play()
    TweenService:Create(BgImage, TweenInfo.new(0.5), {ImageTransparency = 0.2}):Play()
    TweenService:Create(BgOverlay, TweenInfo.new(0.5), {BackgroundTransparency = 0.3}):Play()

    TweenService:Create(LoadingTitle, TweenInfo.new(0.3), {TextTransparency = 0}):Play()
    TweenService:Create(StatusText, TweenInfo.new(0.3), {TextTransparency = 0}):Play()
    TweenService:Create(BarBackground, TweenInfo.new(0.3), {BackgroundTransparency = 0}):Play()

    StatusText.Text = "Welcome"
    task.wait(0.4)
    StatusText.Text = "Game Steal an egg"
    task.wait(0.5)
    StatusText.Text = "Loading game"
    task.wait(0.5)
    StatusText.Text = "Loading script"
    
    local fillTween = TweenService:Create(BarFill, TweenInfo.new(0.8, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        Size = UDim2.new(1, 0, 1, 0)
    })
    fillTween:Play()
    fillTween.Completed:Wait()

    StatusText.Text = "Success"
    task.wait(0.4)

    TweenService:Create(LoadingTitle, TweenInfo.new(0.3), {TextTransparency = 1}):Play()
    TweenService:Create(StatusText, TweenInfo.new(0.3), {TextTransparency = 1}):Play()
    TweenService:Create(BarBackground, TweenInfo.new(0.3), {BackgroundTransparency = 1}):Play()
    TweenService:Create(BarFill, TweenInfo.new(0.3), {BackgroundTransparency = 1}):Play()
    
    TweenService:Create(BgImage, TweenInfo.new(0.4), {ImageTransparency = 0.35}):Play()
    TweenService:Create(BgOverlay, TweenInfo.new(0.4), {BackgroundTransparency = 0.4}):Play()

    task.wait(0.3)
    LoadingFrame:Destroy()

    TopBar.Visible = true
    ContentContainer.Visible = true
end)

-- // LOGIC KÉO THẢ CỬA SỔ
local dragging, dragInput, dragStart, startPos
TopBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

TopBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- // LOGIC KÉO THẢ FLOATING ICON
local fDragging, fDragInput, fDragStart, fStartPos
FloatingIcon.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        fDragging = true
        fDragStart = input.Position
        fStartPos = FloatingIcon.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                fDragging = false
            end
        end)
    end
end)

FloatingIcon.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        fDragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == fDragInput and fDragging then
        local delta = input.Position - fDragStart
        FloatingIcon.Position = UDim2.new(fStartPos.X.Scale, fStartPos.X.Offset + delta.X, fStartPos.Y.Scale, fStartPos.Y.Offset + delta.Y)
    end
end)

-- // NÚT X & XÁC NHẬN
CloseBtn.MouseButton1Click:Connect(function()
    ConfirmOverlay.Visible = true
    TweenService:Create(ConfirmOverlay, TweenInfo.new(0.25), {BackgroundTransparency = 0.5}):Play()
    TweenService:Create(ConfirmBox, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 340, 0, 160),
        BackgroundTransparency = 0
    }):Play()
    TweenService:Create(ConfirmStroke, TweenInfo.new(0.3), {Transparency = 0}):Play()
end)

YesBtn.MouseButton1Click:Connect(function()
    local closeTween = TweenService:Create(MainFrame, TweenInfo.new(0.2, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 0, 0, 0),
        Position = UDim2.new(0.5, 0, 0.5, 0),
        BackgroundTransparency = 1
    })
    TweenService:Create(ConfirmOverlay, TweenInfo.new(0.2), {BackgroundTransparency = 1}):Play()
    TweenService:Create(ConfirmBox, TweenInfo.new(0.2), {Size = UDim2.new(0, 0, 0, 0), BackgroundTransparency = 1}):Play()
    FloatingIcon.Visible = false
    closeTween:Play()
    closeTween.Completed:Wait()
    ScreenGui:Destroy()
end)

NoBtn.MouseButton1Click:Connect(function()
    TweenService:Create(ConfirmOverlay, TweenInfo.new(0.2), {BackgroundTransparency = 1}):Play()
    local boxTween = TweenService:Create(ConfirmBox, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 0, 0, 0),
        BackgroundTransparency = 1
    })
    boxTween:Play()
    boxTween.Completed:Wait()
    ConfirmOverlay.Visible = false
end)

-- // NÚT THU NHỎ (-) & MỞ LẠI
MinimizeBtn.MouseButton1Click:Connect(function()
    local shrinkTween = TweenService:Create(MainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 0, 0, 0),
        BackgroundTransparency = 1
    })
    TweenService:Create(Stroke, TweenInfo.new(0.2), {Transparency = 1}):Play()
    shrinkTween:Play()
    shrinkTween.Completed:Wait()
    MainFrame.Visible = false
    
    FloatingIcon.Visible = true
    FloatingIcon.Size = UDim2.new(0, 0, 0, 0)
    TweenService:Create(FloatingIcon, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 45, 0, 45)
    }):Play()
end)

FloatingIcon.MouseButton1Click:Connect(function()
    local popTween = TweenService:Create(FloatingIcon, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 0, 0, 0)
    })
    popTween:Play()
    popTween.Completed:Wait()
    FloatingIcon.Visible = false
    
    MainFrame.Visible = true
    TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 650, 0, 420),
        BackgroundTransparency = 0
    }):Play()
    TweenService:Create(Stroke, TweenInfo.new(0.4), {Transparency = 0}):Play()
end)
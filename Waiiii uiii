-- Waii Hub - Monochrome Pitch Black Edition (No Settings Tab, Clean Main Tab)
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer

-- Xóa UI cũ nếu có
if CoreGui:FindFirstChild("WaiiHub_VIP") then
    CoreGui.WaiiHub_VIP:Destroy()
end

-- ==========================================
-- 1. XỬ LÝ BẦU TRỜI (SKY)
-- ==========================================
local oldSky = Lighting:FindFirstChildOfClass("Sky")
local customSky = nil

if oldSky then
    oldSky.Parent = nil
end

customSky = Instance.new("Sky")
customSky.Name = "WaiiCustomSky"
customSky.SkyboxBk = "rbxassetid://120937441097851"
customSky.SkyboxDn = "rbxassetid://120937441097851"
customSky.SkyboxFt = "rbxassetid://120937441097851"
customSky.SkyboxLf = "rbxassetid://120937441097851"
customSky.SkyboxRt = "rbxassetid://120937441097851"
customSky.SkyboxUp = "rbxassetid://120937441097851"
customSky.Parent = Lighting

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "WaiiHub_VIP"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true

-- Hàm dọn dẹp khi tắt menu
local function restoreEverythingAndClose()
    if customSky then
        customSky:Destroy()
    end
    if oldSky then
        oldSky.Parent = Lighting
    end
    ScreenGui:Destroy()
end

-- ==========================================
-- 2. MÀN HÌNH LOADING
-- ==========================================
local LoadingFrame = Instance.new("Frame")
LoadingFrame.Name = "LoadingFrame"
LoadingFrame.Parent = ScreenGui
LoadingFrame.AnchorPoint = Vector2.new(0.5, 0.5)
LoadingFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
LoadingFrame.BackgroundTransparency = 0.05
LoadingFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
LoadingFrame.Size = UDim2.new(0, 280, 0, 100)
LoadingFrame.BorderSizePixel = 0
LoadingFrame.ClipsDescendants = true

local LoadCorner = Instance.new("UICorner")
LoadCorner.CornerRadius = UDim.new(0, 18)
LoadCorner.Parent = LoadingFrame

local LoadStroke = Instance.new("UIStroke")
LoadStroke.Color = Color3.fromRGB(255, 255, 255)
LoadStroke.Transparency = 0.3
LoadStroke.Thickness = 1.5
LoadStroke.Parent = LoadingFrame

local LoadIconHolder = Instance.new("Frame")
LoadIconHolder.Parent = LoadingFrame
LoadIconHolder.BackgroundTransparency = 1
LoadIconHolder.Position = UDim2.new(0, 16, 0, 16)
LoadIconHolder.Size = UDim2.new(0, 32, 0, 32)
LoadIconHolder.ClipsDescendants = true

local LoadIconHolderCorner = Instance.new("UICorner")
LoadIconHolderCorner.CornerRadius = UDim.new(0, 8)
LoadIconHolderCorner.Parent = LoadIconHolder

local LoadIcon = Instance.new("ImageLabel")
LoadIcon.Parent = LoadIconHolder
LoadIcon.BackgroundTransparency = 1
LoadIcon.Size = UDim2.new(1, 0, 1, 0)
LoadIcon.Image = "rbxassetid://120937441097851"

local LoadTitle = Instance.new("TextLabel")
LoadTitle.Parent = LoadingFrame
LoadTitle.BackgroundTransparency = 1
LoadTitle.Position = UDim2.new(0, 58, 0, 15)
LoadTitle.Size = UDim2.new(1, -70, 0, 20)
LoadTitle.Font = Enum.Font.GothamBold
LoadTitle.Text = "waii hub"
LoadTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
LoadTitle.TextSize = 16
LoadTitle.TextXAlignment = Enum.TextXAlignment.Left

local LoadSub = Instance.new("TextLabel")
LoadSub.Parent = LoadingFrame
LoadSub.BackgroundTransparency = 1
LoadSub.Position = UDim2.new(0, 58, 0, 35)
LoadSub.Size = UDim2.new(1, -70, 0, 15)
LoadSub.Font = Enum.Font.GothamMedium
LoadSub.Text = "Loading game..."
LoadSub.TextColor3 = Color3.fromRGB(160, 160, 160)
LoadSub.TextSize = 11
LoadSub.TextXAlignment = Enum.TextXAlignment.Left

local BarBg = Instance.new("Frame")
BarBg.Parent = LoadingFrame
BarBg.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
BarBg.BorderSizePixel = 0
BarBg.Position = UDim2.new(0, 20, 0, 70)
BarBg.Size = UDim2.new(1, -40, 0, 4)

local BarBgCorner = Instance.new("UICorner")
BarBgCorner.CornerRadius = UDim.new(1, 0)
BarBgCorner.Parent = BarBg

local BarFill = Instance.new("Frame")
BarFill.Parent = BarBg
BarFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
BarFill.BorderSizePixel = 0
BarFill.Size = UDim2.new(0, 0, 1, 0)

local BarFillCorner = Instance.new("UICorner")
BarFillCorner.CornerRadius = UDim.new(1, 0)
BarFillCorner.Parent = BarFill

-- ==========================================
-- 3. GIAO DIỆN CHÍNH
-- ==========================================
local ToggleIconBtn = Instance.new("TextButton")
ToggleIconBtn.Name = "ToggleIconBtn"
ToggleIconBtn.Parent = ScreenGui
ToggleIconBtn.AnchorPoint = Vector2.new(1, 0)
ToggleIconBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ToggleIconBtn.BackgroundTransparency = 0.2
ToggleIconBtn.Position = UDim2.new(1, -20, 0, 20)
ToggleIconBtn.Size = UDim2.new(0, 95, 0, 32)
ToggleIconBtn.AutoButtonColor = false
ToggleIconBtn.Visible = false
ToggleIconBtn.Text = ""
ToggleIconBtn.ClipsDescendants = true

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 8)
BtnCorner.Parent = ToggleIconBtn

local BtnStroke = Instance.new("UIStroke")
BtnStroke.Color = Color3.fromRGB(255, 255, 255)
BtnStroke.Transparency = 0.3
BtnStroke.Thickness = 1.5
BtnStroke.Parent = ToggleIconBtn

local BtnIconHolder = Instance.new("Frame")
BtnIconHolder.Parent = ToggleIconBtn
BtnIconHolder.BackgroundTransparency = 1
BtnIconHolder.Position = UDim2.new(0, 8, 0.5, -9)
BtnIconHolder.Size = UDim2.new(0, 18, 0, 18)
BtnIconHolder.ClipsDescendants = true

local BtnIconHolderCorner = Instance.new("UICorner")
BtnIconHolderCorner.CornerRadius = UDim.new(0, 4)
BtnIconHolderCorner.Parent = BtnIconHolder

local BtnIcon = Instance.new("ImageLabel")
BtnIcon.Parent = BtnIconHolder
BtnIcon.BackgroundTransparency = 1
BtnIcon.Size = UDim2.new(1, 0, 1, 0)
BtnIcon.Image = "rbxassetid://120937441097851"

local BtnText = Instance.new("TextLabel")
BtnText.Parent = ToggleIconBtn
BtnText.BackgroundTransparency = 1
BtnText.Position = UDim2.new(0, 32, 0, 0)
BtnText.Size = UDim2.new(1, -32, 1, 0)
BtnText.Font = Enum.Font.GothamBold
BtnText.Text = "Waii Hub"
BtnText.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnText.TextSize = 11
BtnText.TextXAlignment = Enum.TextXAlignment.Center

ToggleIconBtn.MouseEnter:Connect(function()
    TweenService:Create(ToggleIconBtn, TweenInfo.new(0.2), {BackgroundTransparency = 0, BackgroundColor3 = Color3.fromRGB(20, 20, 20)}):Play()
end)
ToggleIconBtn.MouseLeave:Connect(function()
    TweenService:Create(ToggleIconBtn, TweenInfo.new(0.2), {BackgroundTransparency = 0.2, BackgroundColor3 = Color3.fromRGB(0, 0, 0)}):Play()
end)

-- Bảng chính
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
MainFrame.BackgroundTransparency = 0.1
MainFrame.Position = UDim2.new(0.5, 0, 0.53, 0)
MainFrame.Size = UDim2.new(0, 0, 0, 0)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Visible = false

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 20)
MainCorner.Parent = MainFrame

local Glow = Instance.new("ImageLabel")
Glow.Name = "Glow"
Glow.Parent = MainFrame
Glow.BackgroundTransparency = 1
Glow.Position = UDim2.new(0, -25, 0, -25)
Glow.Size = UDim2.new(1, 50, 1, 50)
Glow.ZIndex = -1
Glow.Image = "rbxassetid://6015897843"
Glow.ImageColor3 = Color3.fromRGB(255, 255, 255)
Glow.ImageTransparency = 0.85

local UIStroke = Instance.new("UIStroke")
UIStroke.Color = Color3.fromRGB(255, 255, 255)
UIStroke.Transparency = 0.4
UIStroke.Thickness = 2
UIStroke.Parent = MainFrame

local StrokeGradient = Instance.new("UIGradient")
StrokeGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(80, 80, 80)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 255, 255))
})
StrokeGradient.Parent = UIStroke

task.spawn(function()
    local rot = 0
    while MainFrame.Parent do
        rot = (rot + 1.5) % 360
        StrokeGradient.Rotation = rot
        RunService.RenderStepped:Wait()
    end
end)

-- TopBar
local TopBar = Instance.new("Frame")
TopBar.Parent = MainFrame
TopBar.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
TopBar.BackgroundTransparency = 0.2
TopBar.Size = UDim2.new(1, 0, 0, 45)
TopBar.BorderSizePixel = 0
TopBar.ClipsDescendants = true

local TopBarCorner = Instance.new("UICorner")
TopBarCorner.CornerRadius = UDim.new(0, 20)
TopBarCorner.Parent = TopBar

local Title = Instance.new("TextLabel")
Title.Parent = TopBar
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 18, 0, 0)
Title.Size = UDim2.new(0, 300, 1, 0)
Title.Font = Enum.Font.GothamBold
Title.Text = "waii hub <font color='#FFFFFF'>| Monochrome</font>"
Title.RichText = true
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 14
Title.TextXAlignment = Enum.TextXAlignment.Left

-- Kéo thả menu (Draggable)
local dragging, dragInput, dragStart, startPos
TopBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

TopBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(
            startPos.X.Scale, startPos.X.Offset + delta.X,
            startPos.Y.Scale, startPos.Y.Offset + delta.Y
        )
    end
end)

-- Nút Đóng
local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = TopBar
CloseBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
CloseBtn.BackgroundTransparency = 0.2
CloseBtn.AnchorPoint = Vector2.new(1, 0.5)
CloseBtn.Position = UDim2.new(1, -12, 0.5, 0)
CloseBtn.Size = UDim2.new(0, 26, 0, 26)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(220, 220, 220)
CloseBtn.TextSize = 13

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(1, 0)
CloseCorner.Parent = CloseBtn

CloseBtn.MouseButton1Click:Connect(function()
    TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 0, 0, 0)
    }):Play()
    task.wait(0.3)
    restoreEverythingAndClose()
end)

-- Sidebar
local Sidebar = Instance.new("ScrollingFrame")
Sidebar.Parent = MainFrame
Sidebar.Active = true
Sidebar.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
Sidebar.BackgroundTransparency = 0.4
Sidebar.Position = UDim2.new(0, 12, 0, 55)
Sidebar.Size = UDim2.new(0, 140, 1, -67)
Sidebar.CanvasSize = UDim2.new(0, 0, 0, 0)
Sidebar.ScrollBarThickness = 0
Sidebar.BorderSizePixel = 0

local SidebarCorner = Instance.new("UICorner")
SidebarCorner.CornerRadius = UDim.new(0, 12)
SidebarCorner.Parent = Sidebar

local SidebarLayout = Instance.new("UIListLayout")
SidebarLayout.Parent = Sidebar
SidebarLayout.SortOrder = Enum.SortOrder.LayoutOrder
SidebarLayout.Padding = UDim.new(0, 6)

local Container = Instance.new("Frame")
Container.Parent = MainFrame
Container.BackgroundTransparency = 1
Container.Position = UDim2.new(0, 162, 0, 55)
Container.Size = UDim2.new(1, -174, 1, -67)

-- Chỉ giữ lại đúng 1 Tab Main Hub duy nhất (Đã xóa toàn bộ chức năng bên trong & Xóa tab Settings)
local function createTab(name, order)
    local btn = Instance.new("TextButton")
    btn.Parent = Sidebar
    btn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    btn.BackgroundTransparency = 0.1
    btn.Size = UDim2.new(1, 0, 0, 36)
    btn.Font = Enum.Font.GothamMedium
    btn.Text = "   " .. name
    btn.TextColor3 = Color3.fromRGB(0, 0, 0)
    btn.TextSize = 12
    btn.TextXAlignment = Enum.TextXAlignment.Left
    btn.LayoutOrder = order

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 10)
    btnCorner.Parent = btn

    local page = Instance.new("ScrollingFrame")
    page.Parent = Container
    page.BackgroundTransparency = 1
    page.Size = UDim2.new(1, 0, 1, 0)
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.ScrollBarThickness = 3
    page.Visible = true

    local pageLayout = Instance.new("UIListLayout")
    pageLayout.Parent = page
    pageLayout.SortOrder = Enum.SortOrder.LayoutOrder
    pageLayout.Padding = UDim.new(0, 8)

    pageLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        page.CanvasSize = UDim2.new(0, 0, 0, pageLayout.AbsoluteContentSize.Y + 15)
    end)

    return page
end

local MainTab = createTab("Main Hub", 1)

-- Trạng thái ẩn/hiện menu chính
local isOpen = true
ToggleIconBtn.MouseButton1Click:Connect(function()
    isOpen = not isOpen
    if isOpen then
        MainFrame.Visible = true
        TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, 560, 0, 340)
        }):Play()
    else
        TweenService:Create(MainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
            Size = UDim2.new(0, 0, 0, 0)
        }):Play()
        task.wait(0.25)
        MainFrame.Visible = false
    end
end)

-- ==========================================
-- 4. CHẠY HIỆU ỨNG LOADING
-- ==========================================
task.spawn(function()
    TweenService:Create(BarFill, TweenInfo.new(4.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        Size = UDim2.new(1, 0, 1, 0)
    }):Play()
    
    LoadSub.Text = "Loading game..."
    task.wait(0.9)
    
    LoadSub.Text = "Check the code..."
    task.wait(1.0)
    
    LoadSub.Text = "Code security..."
    task.wait(1.1)
    
    LoadSub.Text = "Loading SCRIPT..."
    task.wait(1.0)
    
    LoadSub.Text = "Success!"
    task.wait(0.7)
    
    TweenService:Create(LoadingFrame, TweenInfo.new(0.4), {BackgroundTransparency = 1}):Play()
    for _, child in ipairs(LoadingFrame:GetDescendants()) do
        if child:IsA("TextLabel") then
            TweenService:Create(child, TweenInfo.new(0.4), {TextTransparency = 1}):Play()
        elseif child:IsA("UIStroke") then
            TweenService:Create(child, TweenInfo.new(0.4), {Transparency = 1}):Play()
        elseif child:IsA("ImageLabel") then
            TweenService:Create(child, TweenInfo.new(0.4), {ImageTransparency = 1}):Play()
        elseif child:IsA("Frame") then
            TweenService:Create(child, TweenInfo.new(0.4), {BackgroundTransparency = 1}):Play()
        end
    end
    
    task.wait(0.4)
    LoadingFrame:Destroy()
    
    ToggleIconBtn.Visible = true
    MainFrame.Visible = true
    TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 560, 0, 340)
    }):Play()
    
    print("waii hub Loaded Successfully!")
end)
